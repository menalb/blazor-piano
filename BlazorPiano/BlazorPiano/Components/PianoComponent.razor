@using Model
@for (int i = 3; i < 6; i++)
{
    <OctaveComponent octaveNumber="@BuildOctave(i)"></OctaveComponent>
}

@code{
    private Octave octave = new Octave(1);

    private Octave BuildOctave(int number) => new Octave(number);
}